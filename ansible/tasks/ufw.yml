---
- name: Firewall ufw enabled
  community.general.ufw:
    state: "enabled"
    policy: "deny"
    logging: "on"

- name: Firewall limit ssh
  community.general.ufw:
    rule: limit
    port: ssh
    proto: tcp
